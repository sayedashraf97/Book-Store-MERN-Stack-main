/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

// this Tailwind CSS configuration file sets up Tailwind to analyze specified files for classes, uses the default theme without extension, and does not include additional plugins. It's a basic configuration suitable for many projects, and you can customize it further based on your specific styling needs.