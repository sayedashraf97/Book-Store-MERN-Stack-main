import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
})
// his Vite configuration file is set up for a React project, leveraging the @vitejs/plugin-react plugin to handle React-related features. It's a basic configuration suitable for many React projects, and you can customize it further based on your specific requirements.